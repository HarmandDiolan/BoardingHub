# BoardingHub

Commands to run:

After cloning from github

composer install
cp .env.example .env

edit the .env

After editing .env

php artisan key:generate
php artisan migrate
npm install
npm run dev
php artisan serve
